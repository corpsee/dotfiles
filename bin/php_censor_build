#!/usr/bin/env bash

RELEASE="$1"
test -n "$1" || exit 1;

BUILD_DIR="${HOME}/Build";

cd "${BUILD_DIR}"

composer create-project php-censor/php-censor "php-censor-v${RELEASE}" "${RELEASE}"
tar -czf "php-censor-v${RELEASE}.tar.gz" "php-censor-v${RELEASE}"

rm -R "php-censor-v${RELEASE}"

exit 0;
